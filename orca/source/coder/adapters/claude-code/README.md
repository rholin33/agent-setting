# Claude Code Adapter Notes

Mount `agentroles.coder` as `coder`.

The adapter may project skills into Claude-compatible skill folders or plugin
content. Keep project-specific settings, hook trust, and generated projection
files outside Role source.

This Role is implementation-focused. Formal review, architecture audit, and
release approval should be routed to other roles.

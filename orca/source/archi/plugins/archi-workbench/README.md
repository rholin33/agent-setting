# Archi Workbench Plugin Content

This directory demonstrates role-contained plugin content.

It is not a global plugin install instruction. A host adapter may decide whether
and how to project this content into a role-scoped host-native surface.
